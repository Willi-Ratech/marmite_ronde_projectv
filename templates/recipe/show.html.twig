{% extends 'base.html.twig' %}
{% block title %}Hello DetailController!{% endblock %}

{% block body %}

  <div class="recipe_show">
    <div class="row g-4">
      <div class=" col-xl-3 col-lg-4 col-md-6">
        <div class="card">
          <img src="{{recipe.image}}" class="card-img-top" alt="..."  >
            <div class="card-body">
                <h5 class="card-title">{{ recipe.title }}</h5>
                  <label for="servings">Pour {{ recipe.servings }} Personnes </label>
                  <label for="duration">Temps de préparation  {{ recipe.duration }} min </label>
                  <label for="complication">recette  {{ recipe.complication }} </label>
        

            </div>
        </div>
      </div>
    </div>
  </div>


  

{% endblock %}
