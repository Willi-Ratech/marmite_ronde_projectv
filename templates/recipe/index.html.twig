{% extends 'base.html.twig' %}
{% block title %}Hello ShopController!{% endblock %}


{% block body %}
<div class="container m-4">
  <div class="row g-4">
    {% for recipe in recipes %} 
    <div class=" col-xl-3 col-lg-4 col-md-6">



  <div class="card">
   
    <img src="{{recipe.image}}" class="card-img-top" alt="..."  >
    <div class="card-body">
       <h5 class="card-title">{{ recipe.title }}</h5>
       <label for="servings">Pour {{ recipe.servings }} Personnes </label>
       <label for="duration">Temps de préparation  {{ recipe.duration }} min </label>
       <label for="complication">Complication  {{ recipe.complication }} </label>

        <!-- Bouton Détail -->
        <a href="{{ path('recipe_show', {'id': recipe.id}) }}">
          <button type="button" class="btn btn-primary">Voir les détails</button>
      </a>


    </div>
</div>
    </div>
        {% endfor %}
    </div>
  </div>


  <div class="recipe-show">
    {% for recipe in recipes %}
        <div class="recipe-card">
            









           
           
        </div>
    {% endfor %}
</div>




{% endblock %}


            